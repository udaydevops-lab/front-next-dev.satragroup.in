"use client"
import ReactDatagrid from '@/app/_commonfeatures/ReactDatagrid'
import { PencilIcon } from '@heroicons/react/24/solid';
import { GridColDef } from '@mui/x-data-grid';
import React, { FC } from 'react'
import InactiveIcon from '../../../../../../public/icons/wellness-record/inactive-icon';
import ActiveIcon from '../../../../../../public/icons/wellness-record/active-icon';
import moment from 'moment';
import { getHeaderResponse } from '@/app/_commonfeatures/header';
import services from '@/app/utilities/services';
import { getResulUpdateById, resulStatusUpdate } from '@/app/utilities/api-urls';
import { toast } from 'react-toastify';

interface ResultTamplateMasterGridProps {
    sampleGridData: any,
    getgridData: any,
    setlabServiceFeilsInfo: any,
    popup: any,
    setPopup: any,
    labfeilds: any,
    setLabFeilds: any,
    setBtnType: any,
    handleEdit: any,
}

const ResultTamplateMasterGrid: FC<ResultTamplateMasterGridProps> = ({ sampleGridData, getgridData, setlabServiceFeilsInfo, popup, setPopup, labfeilds, setLabFeilds, setBtnType, handleEdit }) => {


    const columns: GridColDef[] = [
        {
            field: "id",
            headerName: "SNO",
            width: 80,
            // renderCell: (params) => {
            //     const rowNumber = sampleGridData.indexOf(params.row) + 1;
            //     return rowNumber;
            // },
        },
        {
            field: "serviceDesc",
            headerName: "Service Name",
            width: 250,
        },
        {
            field: "resultTemplateName",
            headerName: "Service Description",
            width: 250,
        },
        {
            field: "status",
            headerName: "Status",
            width: 100,
            renderCell: (params: any) => (
                <>
                    {params.row.statusFlag === 1 ?
                        <div className="text-green-600">Active</div>
                        : <div className="text-red-600">Inactive</div>
                    }
                </>
            ),
        },
        {
            field: "createdDate",
            headerName: "Created Date",
            width: 120,
            renderCell: (params: any) => (
                <>
                    {params.row.generatedDate && moment(params.row.generatedDate).format("DD-MM-YYYY")}
                </>
            ),
        },
        {
            field: "generatedBy",
            headerName: "Generated By",
            width: 150,
        },
        {
            field: "modifiedDate",
            headerName: "Modified Date",
            width: 120,
            renderCell: (params: any) => (
                <>
                    {params.row.updatedDate && moment(params.row.updatedDate).format("DD-MM-YYYY")}
                </>
            ),
        },
        {
            field: "updatedBy",
            headerName: "Modified By",
            width: 150,
        },
        // {
        //     field: "lastmodifiedDate",
        //     headerName: "Last Modified Date",
        //     width: 160,
        // },
        {
            field: "action",
            headerName: "Action",
            width: 100,
            renderCell: (params: any) => (
                <div className='flex gap-4 items-center w-[80px] justify-end'>
                    <div className='flex justify-center'>
                        {params.row.statusFlag === 0 ? "" :
                            <PencilIcon
                                onClick={(e: any) => handleEdit(params.row)}
                                className="w-5 h-5 cursor-pointer text-blue-500"
                            />
                        }
                    </div>
                    <div onClick={(e: any) => handleActive(params.row)}>
                        {params.row.statusFlag === 0 ? (
                            <InactiveIcon />
                        ) : (
                            <ActiveIcon />
                        )}
                    </div>
                </div>

            ),
        },
    ];


    const handleActive = (row: any) => {
        //resulStatusUpdate
        console.log(row)
        let Headers = getHeaderResponse()
        let id = row.resultTemplateHeaderId
        let status = row.statusFlag === 1 ? 0 : 1
        let message = row.statusFlag === 1 ? "Successfully inactive the row data" : "Successfully active the row data"
        const postObj = {
            statusFlag: status,
            headerId: id,
        }
        services
            .create(`${resulStatusUpdate}`, postObj, Headers)
            .then((response) => {
                console.log(response.data)
                getgridData()
                toast.success(message)
            })
            .catch((err) => {
                console.log(err.message);
            });
    }

    return (
        <>
            {sampleGridData &&
                <ReactDatagrid
                    rows={sampleGridData} columns={columns}
                />
            }

        </>
    )
}

export default ResultTamplateMasterGrid