import React from 'react'

const ProceduresMasterLayout = ({ children }: { children: React.ReactNode }) => {
    return (
        <>
            {children}
        </>
    )
}

export default ProceduresMasterLayout